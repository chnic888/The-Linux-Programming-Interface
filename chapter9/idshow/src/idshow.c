#define _GNU_SOURCE
#include "idshow.h"
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <grp.h>

#define SG_SIZE (NG)

int idshow(int argc, char* argv[]) {
    uid_t ruid, euid, suid, fsuid;
    gid_t rgid, egid, sgid, fsgid;



    return (EXIT_SUCCESS);
}
